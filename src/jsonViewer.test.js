

const axios = require('./axios');

test('axios function', () => {
    expect(typeof axios).toEqual('function');
});

test('axios get function', () => {
    expect(typeof axios.get).toEqual('function');
});















